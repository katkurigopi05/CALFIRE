interface:
  display_name: "CALFIRE"
  short_description: "Repo-specific patterns and workflows for CALFIRE"
  default_prompt: "Use the CALFIRE repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true